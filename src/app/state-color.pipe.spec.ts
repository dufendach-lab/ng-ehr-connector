import { StateColorPipe } from './state-color.pipe';

describe('StateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
